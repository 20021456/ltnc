#include <bits/stdc++.h>
using namespace std; 
int main() {
    int n;
    cin >> n;
    string s[n];
    getchar();
    for (int i =0; i < n; i++) {
        getline(cin,s[i]);
    }
    int count;
    string s1 = "Judas Iscariot";
    size_t found = s2.find(s1);
    s.erase(s.begin())
    for (int i = 0; i < n-1; i++) {
        cout << s[i] << " ";
    }
}